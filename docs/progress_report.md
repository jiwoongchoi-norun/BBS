# BBS 프로젝트 진행 보고서

## 1. 프로젝트 개요

이 프로젝트는 Node.js, Express, EJS, OracleDB 기반 게시판 과제 프로젝트다. 교수님 PPT 요구사항의 필수 게시판 흐름을 우선 구현하고, 보안 및 편의 기능을 추가 점수 후보로 보강하는 방향으로 진행 중이다.

## 2. 현재 구현 결과

현재 코드 기준 구현 범위는 다음과 같다.

- 게시글 목록
- 글쓰기
- 글읽기
- 글수정
- 글삭제
- 검색
- 조회수
- 페이징
- 로그인
- 로그아웃
- 회원가입
- 회원정보 수정
- 세션 처리
- bcrypt 비밀번호 저장 및 로그인 검증
- 기존 SHA-512 + salt 계정 로그인 성공 시 bcrypt 자동 전환
- 댓글 작성
- 대댓글 작성
- 본인 댓글 삭제
- 게시글 좋아요/싫어요 및 추천 취소
- 파일 업로드
- 파일 다운로드
- Bootstrap 5 기반 반응형 UI
- `.env` 기반 환경설정 분리
- `SESSION_SECRET` 환경변수 사용
- 게시글 soft delete 처리
- 작성자 기준 게시글 수정/삭제 권한 체크
- 주요 SQL bind variable 적용
- 기본 입력값 검증
- ESLint/Prettier/npm scripts 기반 검증 루틴

## 3. 주요 개선 사항

### 3.1 인증과 세션

- 로그인 성공 시 `req.session.user`에 사용자 ID를 저장한다.
- 비밀번호는 세션에 저장하지 않는다.
- 로그인 필요 기능은 `requireLogin()`으로 보호한다.
- 회원정보 수정 화면에서는 기존 비밀번호 값을 다시 노출하지 않는다.

### 3.2 비밀번호 저장

- 회원가입과 회원정보 수정 시 bcrypt hash를 생성한다.
- 신규 계정은 `LOGIN.PASSWORD_ALGO = 'bcrypt'`, `LOGIN.SALT = NULL`로 저장한다.
- 기존 SHA-512 + salt 계정은 로그인 성공 시 bcrypt로 자동 재저장한다.
- `PASSWORD_ALGO`로 기존 계정과 신규 bcrypt 계정을 구분한다.

### 3.3 SQL Injection 완화

- 로그인, 회원가입, 회원정보 수정, 게시글 CRUD, 검색, 댓글, 대댓글, 파일 다운로드에 Oracle bind variable을 적용했다.
- 숫자 query/body 값은 양의 정수인지 검사한다.
- 검색 컬럼은 허용 목록으로 제한한다.

### 3.4 권한 체크

- 게시글 수정/삭제는 로그인 사용자와 `BBS.WRITER`가 일치해야 한다.
- 댓글 삭제는 로그인 사용자와 `BBSW.WRITER`가 일치해야 한다.
- 권한이 없으면 403 응답을 반환한다.

### 3.5 페이징

- 목록과 검색 결과에 `OFFSET ... FETCH NEXT` 기반 페이징을 적용했다.
- 화면에는 이전/다음 및 페이지 번호를 표시한다.

### 3.6 댓글과 대댓글

- `BBSW` 테이블을 기준으로 댓글을 저장한다.
- `PARENT_NO`와 `DEPTH`를 사용해 대댓글을 표시한다.
- 현재 댓글 수정은 미구현이다.

### 3.7 좋아요/싫어요

- `BBS.LIKE_COUNT`, `BBS.DISLIKE_COUNT`에 게시글별 카운트를 저장한다.
- `BBS_REACTION` 테이블의 `(BBSNO, USER_ID)` 기본키로 동일 사용자의 중복 추천을 방지한다.
- 같은 버튼을 다시 누르면 추천이 취소된다.
- 반대 버튼을 누르면 기존 추천이 전환된다.

### 3.8 파일 업로드

- `multer`를 사용해 `uploads/bbs`에 파일을 저장한다.
- `BBS_FILE` 테이블에 원본 파일명, 저장 파일명, 경로, 크기, MIME 타입을 저장한다.
- 허용 확장자와 10MB 파일 크기 제한을 적용한다.
- 다운로드 시 DB의 저장 파일명을 기준으로 서버 파일을 내려준다.

## 4. 기능별 진행 현황

| 항목               | 상태   |
| ------------------ | ------ |
| 게시글 목록        | 완료   |
| 글쓰기             | 완료   |
| 글읽기             | 완료   |
| 글수정             | 완료   |
| 글삭제             | 완료   |
| 검색               | 완료   |
| 조회수             | 완료   |
| 페이징             | 완료   |
| 로그인             | 완료   |
| 로그아웃           | 완료   |
| 회원가입           | 완료   |
| 회원정보 수정      | 완료   |
| 세션 처리          | 완료   |
| 비밀번호 암호화    | 완료   |
| 댓글               | 완료   |
| 대댓글             | 완료   |
| 댓글 삭제          | 완료   |
| 좋아요/싫어요 처리 | 완료   |
| 파일 업로드        | 완료   |
| 파일 다운로드      | 완료   |
| Bootstrap UI 개선  | 완료   |
| 댓글 수정          | 미완료 |
| 관리자 기능        | 미완료 |
| 자동화 테스트      | 미완료 |

## 5. DB 구성

현재 스키마 기준 주요 객체는 다음과 같다.

- `LOGIN`
- `BBS`
- `BBSW`
- `BBS_REACTION`
- `BBS_FILE`
- `BBS_SEQ`
- `BBSW_SEQ`
- `BBS_FILE_SEQ`

신규 DB에는 `scripts/schema.sql`을 실행하고, 샘플 데이터가 필요하면 `scripts/sample-data.sql`을 실행한다. 기존 DB는 `scripts/migration.sql`로 통합 보강한다.

## 6. 남은 과제

1. `npm run lint`, `npm run format:check`, `npm run verify:app` 검증
2. OracleDB에서 스키마 스크립트 실행 확인
3. 기존 SHA-512 계정의 bcrypt 자동 전환 확인
4. 좋아요/싫어요 수동 테스트
5. 댓글 수정 기능 구현
6. 제출용 화면 캡처 정리

## 7. 종합 평가

현재 프로젝트는 과제 필수 기능 대부분과 주요 가산점 후보를 시연 가능한 상태까지 구현했다. 페이징, 댓글, 파일 업로드, bcrypt, 좋아요/싫어요, SQL bind variable, 작성자 권한 체크가 반영되어 제출용 완성도가 높아졌다. 남은 작업은 댓글 수정, 관리자 기능, 자동화 테스트와 실제 OracleDB 실행 검증이다.
